---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: experiments.fis.fis.dksshddl.dev
spec:
  group: fis.fis.dksshddl.dev
  names:
    kind: Experiment
    listKind: ExperimentList
    plural: experiments
    shortNames:
    - fisexp
    singular: experiment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.experimentId
      name: Experiment ID
      type: string
    - jsonPath: .spec.experimentTemplate.name
      name: Template
      type: string
    - jsonPath: .spec.schedule
      name: Schedule
      type: string
    - jsonPath: .status.lastScheduleTime
      name: Last Schedule
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Experiment is the Schema for the experiments API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Experiment
            properties:
              clientToken:
                description: |-
                  ClientToken is an optional unique identifier for the experiment
                  If not provided, one will be generated automatically
                type: string
              experimentTemplate:
                description: |-
                  ExperimentTemplate specifies which template to use
                  Either ID or Name must be specified
                properties:
                  id:
                    description: |-
                      ID is the AWS FIS experiment template ID (e.g., "EXT1234567890abcdef")
                      Either ID or Name must be specified
                    type: string
                  name:
                    description: |-
                      Name is the name of the ExperimentTemplate CRD in the same namespace
                      Either ID or Name must be specified
                    type: string
                type: object
              failedExperimentsHistoryLimit:
                default: 1
                description: |-
                  FailedExperimentsHistoryLimit is the number of failed finished experiments to retain
                  Default is 1
                format: int32
                minimum: 0
                type: integer
              schedule:
                description: |-
                  Schedule defines when to run the experiment (cron expression)
                  If not specified, the experiment runs once immediately (Job mode)
                  Examples: "0 2 * * *" (daily at 2am), "*/30 * * * *" (every 30 minutes)
                type: string
              successfulExperimentsHistoryLimit:
                default: 3
                description: |-
                  SuccessfulExperimentsHistoryLimit is the number of successful finished experiments to retain
                  Default is 3
                format: int32
                minimum: 0
                type: integer
              suspend:
                description: |-
                  Suspend tells the controller to suspend subsequent executions
                  This does not apply to already started experiments
                type: boolean
              tags:
                description: Tags to apply to the experiment
                items:
                  description: Tag represents a key-value pair for tagging resources
                  properties:
                    key:
                      description: Key is the tag key
                      maxLength: 128
                      minLength: 1
                      type: string
                    value:
                      description: Value is the tag value
                      maxLength: 256
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - experimentTemplate
            type: object
          status:
            description: status defines the observed state of Experiment
            properties:
              active:
                description: Active is the number of currently running experiments
                format: int32
                type: integer
              conditions:
                description: Conditions represent the current state of the Experiment
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              endTime:
                description: EndTime is when the experiment ended
                format: date-time
                type: string
              experimentId:
                description: ExperimentID is the AWS FIS experiment ID
                type: string
              lastScheduleTime:
                description: LastScheduleTime is the last time the experiment was
                  scheduled (for scheduled experiments)
                format: date-time
                type: string
              nextScheduleTime:
                description: NextScheduleTime is the next time the experiment will
                  be scheduled (for scheduled experiments)
                format: date-time
                type: string
              reason:
                description: Reason provides additional information about the current
                  state
                type: string
              startTime:
                description: StartTime is when the experiment started
                format: date-time
                type: string
              state:
                description: |-
                  State represents the current state of the experiment
                  Possible values: initiating, pending, running, completed, stopping, stopped, failed
                type: string
              targetAccountConfigurationsCount:
                description: TargetAccountConfigurationsCount is the number of target
                  account configurations
                format: int64
                type: integer
              templateId:
                description: TemplateID is the resolved AWS FIS template ID
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
